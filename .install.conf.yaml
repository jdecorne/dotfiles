- defaults:
    link:
      create: true
      relink: true
- clean: ['~']
- link:
    ~/.zsh:
    ~/.zshrc:
    ~/.tmux.conf:
- shell:
  - ["git submodule update --init --recursive", "Installing submodules"]
