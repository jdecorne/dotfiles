- defaults:
    link:
      create: true
      relink: true
- clean: ['~', '~/.config']
- link:
    ~/.zsh:
    ~/.zshrc:
- shell:
  - ["git submodule update --init --recursive", "Installing submodules"]
